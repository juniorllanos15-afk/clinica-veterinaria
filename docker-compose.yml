services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: clinica_veterinaria_app
    restart: unless-stopped
    working_dir: /var/www/html
    ports:
      - "5173:5173"
    volumes:
      - ./:/var/www/html
    networks:
      - clinica_network
    environment:
      - PHP_IDE_CONFIG=serverName=clinica-veterinaria
    depends_on:
      - db

  caddy:
    image: caddy:2-alpine
    container_name: clinica_veterinaria_caddy
    restart: unless-stopped
    ports:
      - "8000:80"
      - "8443:443"
    volumes:
      - ./:/var/www/html
      - ./docker/Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    networks:
      - clinica_network
    depends_on:
      - app

  db:
    image: postgres:15-alpine
    container_name: clinica_veterinaria_db
    restart: unless-stopped
    environment:
      POSTGRES_DB: clinica_veterinaria
      POSTGRES_USER: clinica_user
      POSTGRES_PASSWORD: clinica_secret
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    networks:
      - clinica_network

networks:
  clinica_network:
    driver: bridge

volumes:
  db_data:
  caddy_data:
  caddy_config:
